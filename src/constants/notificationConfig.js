/**
 * Notification and UI Configuration
 * Centralized timeout constants and UI behavior
 */

export const NOTIFICATION_TIMEOUT = 2500; // milliseconds

export const SCROLL_LOCK_DELAY = 100; // milliseconds

export const FILTER_BUTTONS = {
  all: 'All',
  unread: 'Unread',
  archived: 'Archived',
};
